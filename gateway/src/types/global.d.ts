export {}; declare global { interface Window { casperWallet?: { requestConnection: () => Promise<void>; getActivePublicKey: () => Promise<string>; signMessage: (msg: string, pub: string) => Promise<string | object>; sign: (deployJson: string, pub: string) => Promise<{ cancelled?: boolean; signature: Uint8Array; signatureHex?: string }>; isConnected: () => Promise<boolean>; } } }
