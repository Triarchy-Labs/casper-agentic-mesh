[package]
name = "odra-mesh-oracle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
odra = { version = "0.8.0" }
odra-modules = { version = "0.8.0" }

[build-dependencies]
odra-build = "0.8.0"
